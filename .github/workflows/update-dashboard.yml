name: 대시보드 자동 업데이트

on:
  schedule:
    # 실시간 지수: 5분마다 (장중 시간대만)
    - cron: '*/5 0-8 * * 1-5'  # UTC 00:00-08:00 = 한국 09:00-17:00 (평일)
    # 공식 지표: 하루 1회 (오전 9시)
    - cron: '0 0 * * *'  # UTC 00:00 = 한국 09:00
  
  # 수동 실행 가능
  workflow_dispatch:
  
  # Push 시에도 실행
  push:
    branches: [ main ]

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 코드 체크아웃
      - name: 체크아웃
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      # 2. Python 설정
      - name: Python 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      # 3. 패키지 설치
      - name: 패키지 설치
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      # 4-1. 실시간 지수 업데이트 (5분마다)
      - name: 실시간 지수 업데이트
        run: |
          echo "🚀 실시간 지수 업데이트 시작..."
          python auto_update_all_indicators.py || echo "⚠️  일부 실패했지만 계속 진행"
          echo "✅ 실시간 지수 업데이트 완료"
        continue-on-error: true
      
      # 4-2. 공식 지표 업데이트 (하루 1회 또는 수동/Push)
      - name: 공식 지표 업데이트
        if: github.event.schedule == '0 0 * * *' || github.event_name == 'workflow_dispatch' || github.event_name == 'push'
        run: |
          echo "🏛️  공식 지표 업데이트 시작..."
          python auto_update_official_data.py || echo "⚠️  일부 실패했지만 계속 진행"
          echo "✅ 공식 지표 업데이트 완료"
        env:
          BOK_API_KEY: ${{ secrets.BOK_API_KEY }}
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        continue-on-error: true
      
      # 5. 변경사항 확인 및 커밋
      - name: 변경사항 확인
        id: check_changes
        run: |
          if [[ -n $(git status -s) ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "✅ 변경사항이 있습니다"
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "ℹ️  변경사항이 없습니다"
          fi
      
      # 6. 커밋 및 푸시 (변경사항이 있을 때만)
      - name: 변경사항 커밋 및 푸시
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add index.html
          git commit -m "🔄 자동 업데이트: $(TZ=Asia/Seoul date +'%Y-%m-%d %H:%M:%S KST')"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # 7. 완료 메시지
      - name: 완료
        run: |
          echo "========================================"
          echo "✅ 모든 작업이 완료되었습니다!"
          echo "⏰ 완료 시간: $(TZ=Asia/Seoul date +'%Y-%m-%d %H:%M:%S KST')"
          echo "========================================"
