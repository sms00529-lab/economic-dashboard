name: 대시보드 자동 업데이트

on:
  schedule:
    # 실시간 지수: 5분마다 (장중 시간대)
    - cron: '*/5 0-8 * * 1-5'  # 한국시간 09:00-17:00 (평일)
    # 공식 지표: 하루 1회
    - cron: '0 0 * * *'  # 한국시간 09:00
  
  # 수동 실행 가능
  workflow_dispatch:
  
  # Push 시에도 실행
  push:
    branches: [ main ]

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 코드 체크아웃
      - name: 체크아웃
        uses: actions/checkout@v3
      
      # 2. Python 설정
      - name: Python 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      # 3. 패키지 설치
      - name: 패키지 설치
        run: |
          pip install -r requirements.txt
      
      # 4-1. 실시간 지수 업데이트 (5분마다)
      - name: 실시간 지수 업데이트 (KOSPI, NASDAQ, Bitcoin...)
        run: |
          python auto_update_all_indicators.py
          echo "✅ 실시간 지수 업데이트 완료"
      
      # 4-2. 공식 지표 업데이트 (임시 비활성화 - 수동 관리)
      # - name: 공식 지표 업데이트 (금리, 국채, M2, GDP, CPI)
      #   if: github.event.schedule == '0 0 * * *' || github.event_name == 'workflow_dispatch' || github.event_name == 'push'
      #   run: |
      #     python auto_update_official_data.py
      #     echo "✅ 공식 지표 업데이트 완료"
      
      # 5. 변경사항 커밋
      - name: 변경사항 커밋 및 푸시
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add index.html
          git diff --staged --quiet || git commit -m "🔄 자동 업데이트: $(date +'%Y-%m-%d %H:%M:%S')"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
