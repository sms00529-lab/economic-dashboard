name: 6개 주요 지수 자동 업데이트

on:
  schedule:
    # 평일 장중 5분마다 (한국시간 09:00-17:00)
    - cron: '*/5 0-8 * * 1-5'
  
  # 수동 실행 가능
  workflow_dispatch:
  
  # Push 시에도 실행
  push:
    branches: [ main ]

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 코드 체크아웃
      - name: 체크아웃
        uses: actions/checkout@v3
      
      # 2. Python 설정
      - name: Python 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      # 3. 패키지 설치
      - name: 패키지 설치
        run: |
          pip install -r requirements.txt
      
      # 4. 6개 주요 지수 업데이트
      - name: 6개 주요 지수 업데이트
        run: |
          python update_6indicators.py
      
      # 5. 변경사항 커밋
      - name: 변경사항 커밋 및 푸시
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add index.html
          git diff --staged --quiet || git commit -m "🔄 6개 주요 지수 자동 업데이트: $(date +'%Y-%m-%d %H:%M:%S')"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
